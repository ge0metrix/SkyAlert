[project]
name = "skyalert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "textual>=6.1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
